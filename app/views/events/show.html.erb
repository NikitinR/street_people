<div class="event-single">
  <%= image_tag @event.image.url(:post).to_s %>
  <h1>
      <%= @event.name %>
  </h1>
  <%= raw(@event.description) %>

  <%= link_to 'Back to Events', events_path, :class => "join" %>


  <%= render 'disqus/thread' %>


</div>