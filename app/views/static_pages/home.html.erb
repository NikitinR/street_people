<h1>Home page</h1>


<h1>Life stories </h1>

<ul>
  <% @posts.each do |post| %>
      <li>
        <h2><%= post.title %></h2>
        <%= image_tag post.image.url(:thumb).to_s %>
        <p><%= post.text %></p>
      </li>
  <% end %>
</ul>
